# Ai-prompt-marketplace
strict schema to validate prompts (inputs, constraints, safety tags, expected outputs, versioning, pricing tiers).
Sellable: versioning + license + pricing baked in
Composable: inputs + output schema = UI auto-generation
Safe-by-design: risk levels + red-team notes
Marketplace-ready: validation, previews, trust signals
